services:
  - type: web
    name: acme-policy-rag
    runtime: docker
    region: oregon
    plan: free
    healthCheckPath: /health
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false          # Set manually in Render dashboard
      - key: OPENROUTER_MODEL
        value: deepseek/deepseek-r1-0528:free
      - key: FLASK_DEBUG
        value: "false"
